<p id="notice"><%= notice %></p>

<h3 class='text-center'>Photos for #<%= @tag.name %></h3>

<div id='photos-holder'>
	<% @tag.photos.order("created_at desc").limit(40).each do |photo| %>
		<%= link_to image_tag(photo.low_resolution), photo.link %>
	<% end %>
</div>